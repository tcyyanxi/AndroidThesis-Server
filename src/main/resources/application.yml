server:
  port: 2388
  servlet:
    context-path: /tancy

spring:
  security:
    user:
      name: admin
      password: 1111
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/thesis?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    com.example: debug
mybatis-plus:
  mapper-locations: classpath:/mappers/*.xml
  type-aliases-package: com.example.androidthesisaerver.entity

